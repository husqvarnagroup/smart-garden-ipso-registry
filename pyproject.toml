# SPDX-FileCopyrightText: 2026 GARDENA GmbH
#
# SPDX-License-Identifier: GPL-3.0-or-later

# This file is kept minimal because it is used for dev dependencies only, not
# for publishing.

[dependency-groups]
dev = [
    "pre-commit>=4.6.0",
    "reuse>=6.2.0",
]
